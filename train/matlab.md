# MATLAB — plano de treinamento (o que precisa constar)

**Objetivo**: cobrir tudo pra o expert responder qualquer pergunta sobre MATLAB (engenharia/numérico).
**Expert sugerido**: família em `data_science_experts`. **Total est.**: ~70 lições.
**Convenção**: `treinamento_matlab/<família>/<subsetor>/*.md` → path = [família, subsetor].

## core/ — ~26
o ambiente MATLAB; variáveis e workspace; escalares, vetores e matrizes; criação de matrizes; indexação; operações element-wise vs matriciais; operadores; strings e char arrays; cell arrays; structs; controle de fluxo (`if`/`switch`); loops (`for`/`while`); funções (arquivos e anônimas); scripts vs funções; `varargin`/`varargout`; broadcasting; lógicos e máscaras.

## numerico-graficos/ — ~24
álgebra linear; sistemas lineares; funções matemáticas; estatística; polinômios; interpolação; integração numérica; equações diferenciais (ode45); FFT; plot 2D; plot 3D; subplots; customização de gráficos; anotações; histogramas; superfícies; animações; exportar figuras; ajuste de curvas (fit); optimization.

## toolboxes-avancado/ — ~20
Simulink (visão geral); Signal Processing Toolbox; Image Processing; Statistics/ML Toolbox; Deep Learning Toolbox; Control Systems; Symbolic Math; leitura de dados (CSV/Excel); tabelas (table); app designer/GUI; performance e vetorização; pré-alocação; profiler; debugging; classes (OOP); interop (Python/C); geração de código; boas práticas.
