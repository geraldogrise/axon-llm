# Haxe — plano de treinamento (o que precisa constar)

**Objetivo**: cobrir tudo pra o expert responder qualquer pergunta sobre Haxe (multiplataforma).
**Expert sugerido**: família em `crossplatform_experts`. **Total est.**: ~45 lições.
**Convenção**: `treinamento_haxe/<família>/<subsetor>/*.md` → path = [família, subsetor].

## core/ — ~24
o que é Haxe (compila pra várias linguagens); sintaxe; variáveis e tipos; type inference; operadores; strings; controle de fluxo; loops; funções; parâmetros; arrays; maps; classes; herança; interfaces; enums (algébricos); pattern matching (`switch`); abstract types; typedef; null safety; comentários.

## avancado-ecossistema/ — ~21
generics; type parameters; macros (metaprogramação); anonymous structures; extension methods; static extensions; properties; getters/setters; targets (JS/C++/C#/Java/Python/PHP); condicional compilation; haxelib (pacotes); OpenFL/Heaps (games); testes; interop com targets; performance; NME/Lime; comparação com outras linguagens; boas práticas.
