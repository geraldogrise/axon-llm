# Prolog — plano de treinamento (o que precisa constar)

**Objetivo**: cobrir tudo pra o expert responder qualquer pergunta sobre Prolog (lógica).
**Expert sugerido**: família em `functional_experts` ou `logic_experts`. **Total est.**: ~45 lições.
**Convenção**: `treinamento_prolog/<família>/<subsetor>/*.md` → path = [família, subsetor].

## core/ — ~24
o que é programação lógica; fatos; regras; consultas (queries); unificação; variáveis; átomos e termos; a resolução (backtracking); operador `:-`; conjunção e disjunção; negação (`\+`); listas; recursão sobre listas; pattern matching; aritmética (`is`); comparação; cut (`!`); controle de backtracking; findall/bagof/setof; assert/retract.

## avancado-ecossistema/ — ~21
estruturas de dados; árvores; grafos; DCG (Definite Clause Grammars); parsing; constraint logic programming (CLP); resolução de puzzles; sistemas especialistas; meta-interpreters; SWI-Prolog; I/O; módulos; debugging (trace); performance; tabling; comparação com programação declarativa; casos de uso (IA/NLP); boas práticas.
