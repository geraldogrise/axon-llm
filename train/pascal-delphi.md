# Pascal / Delphi — plano de treinamento (o que precisa constar)

**Objetivo**: cobrir tudo pra o expert responder qualquer pergunta sobre Pascal e Delphi (Object Pascal).
**Expert sugerido**: família em `legacy_experts`. **Total est.**: ~55 lições.
**Convenção**: `treinamento_pascal/<família>/<subsetor>/*.md` → path = [família, subsetor].

## pascal/ — ~26
o que é Pascal; estrutura de um programa; variáveis e tipos; constantes; operadores; entrada/saída (Read/Write); controle de fluxo (if/case); loops (for/while/repeat); procedures; functions; parâmetros (var/const); escopo; arrays; strings; records; sets; enumerations; ponteiros; alocação dinâmica; recursão; units; tipos definidos pelo usuário; arquivos.

## delphi-oop/ — ~18
Object Pascal e OOP; classes; construtores/destrutores; propriedades; herança; polimorfismo; interfaces; visibilidade; exceptions (try/except/finally); generics; RTTI; class methods; virtual/override; abstract; anonymous methods; gerenciamento de memória.

## vcl-ecossistema/ — ~11
VCL (interface visual); forms e componentes; eventos; RAD (visual designer); FireMonkey (FMX); banco de dados (FireDAC); DataSets; unit testing (DUnit); Free Pascal/Lazarus; packages; multi-plataforma; boas práticas.
